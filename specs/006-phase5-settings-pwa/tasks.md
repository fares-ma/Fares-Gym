# Tasks: Phase 5 — Settings, Export & PWA

- [x] T001: Configure PWA manifest in `public/manifest.json` and link in `app/layout.tsx`
- [x] T002: Add Settings and Backup Arabic translations in `src/i18n/ar.ts`
- [x] T003: Implement `src/server/settings-queries.ts` (diagnostics, unit tags, backup metadata)
- [x] T004: Implement `src/server/settings-actions.ts` (`exportAllDataAction`, `updateAppSettingAction`, `confirmUnitTagAction`)
- [x] T005: Build `BackupExportCard.tsx` with instant JSON download
- [x] T006: Build `WeightNotationCard.tsx` and `SystemInfoCard.tsx`
- [x] T007: Assemble `SettingsView.tsx` and update `app/(app)/settings/page.tsx`
- [x] T008: Verify build (`npm run build`) and domain tests (`npm run test:domain`)
